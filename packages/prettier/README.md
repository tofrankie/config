# @tofrankie/prettier

Based on [Prettier](https://prettier.io/docs/configuration).

## Quick Start

```bash
$ pnpm add prettier @tofrankie/prettier -D
```

Create a `prettier.config.js` in your project root:

```js
export default '@tofrankie/prettier'
```
